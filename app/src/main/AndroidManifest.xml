<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kangaroos.youwill">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YouWill">
        <activity android:name=".LetterPost"></activity>
        <activity android:name=".WillPost" />

        <service
            android:name=".MyFirebaseMessagingService"
            android:stopWithTask="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".MyPageDetail" />
        <activity android:name=".HealingPost" />
        <activity android:name=".HealingWrite" />
        <activity android:name=".Healing" />
        <activity android:name=".LetterChoice" />
        <activity android:name=".LetterWrite" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".WillFirstInfo" />
        <activity android:name=".WillWrite" />
        <activity android:name=".MyPage" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>